rules_version = '2';

service cloud.firestore {
    match /databases/{database}/documents {
        match /users/{userId} {
            match /{anyCollection=**}/{anyItem} {
                allow read: if request.auth.uid == userId;
            }

            match /development/{anyItem} {
                allow write: if request.auth.uid == userId;
            }
        }
    }
}
